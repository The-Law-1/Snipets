{
	"name": "context-menu-ts-extension",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "esbuild src/background.ts --bundle --platform=browser --outfile=dist/background.js && esbuild src/options.ts --bundle --platform=browser --outfile=dist/options.js",
		"clean": "rimraf dist",
		"prettier": "prettier --write ."
	},
	"devDependencies": {
		"@types/chrome": "^0.1.6",
		"esbuild": "^0.21.5",
		"prettier": "3.6.2",
		"rimraf": "^6.0.1",
		"typescript": "^5.5.4"
	}
}
